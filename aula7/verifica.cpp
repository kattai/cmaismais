#include <stdio.h>
#include <iostream>

using namespace std;

bool verifica(int x, int tam){
    for(int i = 0; i <= tam; i++){
        if(x == A[tam])
            return true;
    }

    return false;
}